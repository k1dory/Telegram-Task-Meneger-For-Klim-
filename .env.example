# ===========================================
# Telegram Task Manager - Environment Variables
# ===========================================
# Copy this file to .env and fill in the values
# cp .env.example .env

# ===========================================
# Telegram Configuration
# ===========================================
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_MINI_APP_URL=https://your-domain.com
VITE_BOT_USERNAME=your_bot_username

# ===========================================
# Database (PostgreSQL)
# ===========================================
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=taskmanager
POSTGRES_PASSWORD=change_me_in_production
POSTGRES_DB=taskmanager
DATABASE_URL=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}?sslmode=disable

# ===========================================
# Backend Configuration
# ===========================================
API_PORT=8080
JWT_SECRET=change_this_to_a_secure_random_string_at_least_32_chars
GIN_MODE=debug
# GIN_MODE=release  # Use in production

# ===========================================
# Frontend Configuration
# ===========================================
VITE_API_URL=http://localhost:8080/api
# VITE_API_URL=/api  # Use in production with nginx proxy

# ===========================================
# Notifications
# ===========================================
NOTIFICATION_CHECK_INTERVAL=30m
DEFAULT_REMINDER_HOURS=6,8,12

# ===========================================
# Docker Configuration (Production)
# ===========================================
# GitHub Container Registry
DOCKER_REGISTRY=ghcr.io/your-github-username
VERSION=latest

# Domain for SSL certificate
DOMAIN=your-domain.com

# ===========================================
# CI/CD Notifications (Optional)
# ===========================================
# Telegram bot for deployment notifications
# TELEGRAM_NOTIFY_BOT_TOKEN=your_notify_bot_token
# TELEGRAM_NOTIFY_CHAT_ID=your_chat_id
